/*
Como você pode usar uma declaração if para verificar se uma 
string está vazia em JavaScript?
*/

let minhaString = "";

if (minhaString === "") {
    console.log("A string está vazia.");
} else {
    console.log("A string não está vazia.");
}